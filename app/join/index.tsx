import { BetaWaitlistFlow } from '@/components/beta/BetaWaitlistFlow';

/** Public beta waitlist at `/join` — standalone, no auth. */
export default function JoinScreen() {
  return <BetaWaitlistFlow />;
}
