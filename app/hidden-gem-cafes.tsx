import { PublicLandingPage } from '@/components/public/PublicLandingPage';
import { HIDDEN_GEM_CAFES_PAGE } from '@/lib/publicLandingConfig';

export default function HiddenGemCafesPage() {
  return <PublicLandingPage config={HIDDEN_GEM_CAFES_PAGE} />;
}
